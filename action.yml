name: 'Cost Guardrails'
description: 'Shift-left cost governance for Terraform and CloudFormation. Reviews infrastructure costs on every PR.'
author: 'cost-guardrails'

branding:
  icon: 'dollar-sign'
  color: 'green'

inputs:
  plan-path:
    description: 'Path to Terraform plan JSON or CloudFormation changeset JSON'
    required: false
    default: 'plan.json'
  budget-code:
    description: 'Budget code for cost validation (e.g. CS-FY2026-BU105-M03)'
    required: false
    default: ''
  api-key:
    description: 'Cost Guardrails API key'
    required: true
  github-token:
    description: 'GitHub token for posting PR comments'
    required: false
    default: ${{ github.token }}
  skip-budget:
    description: 'Skip budget validation (pricing-only mode)'
    required: false
    default: 'false'
  skip-narrative:
    description: 'Skip AI analysis (faster runs)'
    required: false
    default: 'false'
  skip-guardrails:
    description: 'Skip guardrail evaluation'
    required: false
    default: 'false'
  auto-approve:
    description: 'Pass pipeline when no budget is found'
    required: false
    default: 'false'
  format:
    description: 'Comment format: markdown or terminal'
    required: false
    default: 'markdown'
  post-comment:
    description: 'Post cost breakdown as PR comment'
    required: false
    default: 'true'
  api-url:
    description: 'Cost Guardrails API URL (override for custom deployments)'
    required: false
    default: 'https://4tm9xj5nv0.execute-api.us-east-1.amazonaws.com/rnd'
  tag:
    description: 'Tag to identify this comment (e.g. production, staging). Each tag gets its own PR comment.'
    required: false
    default: ''
  extra-args:
    description: 'Extra CLI flags (advanced escape hatch)'
    required: false
    default: ''

outputs:
  decision:
    description: 'Cost Guardrails decision: ALLOW, WARN, or BLOCK'
    value: ${{ steps.cost-guardrails.outputs.decision }}
  monthly-cost:
    description: 'Estimated monthly cost'
    value: ${{ steps.cost-guardrails.outputs.monthly_cost }}
  result-file:
    description: 'Path to the cached result JSON'
    value: ${{ steps.cost-guardrails.outputs.result_file }}
  report-file:
    description: 'Path to the HTML report'
    value: ${{ steps.cost-guardrails.outputs.report_file }}

runs:
  using: 'composite'
  steps:
    - name: Install Cost Guardrails CLI
      shell: bash
      run: pip install --quiet cost-guardrails

    - name: Run Cost Guardrails
      id: cost-guardrails
      shell: bash
      env:
        COST_GUARDRAILS_API_KEY: ${{ inputs.api-key }}
        COST_GUARDRAILS_API_URL: ${{ inputs.api-url }}
        GITHUB_TOKEN: ${{ inputs.github-token }}
      run: |
        # Build CLI flags
        OPTS=""
        [ "${{ inputs.skip-budget }}" = "true" ] && OPTS="$OPTS --skip-budget"
        [ "${{ inputs.skip-narrative }}" = "true" ] && OPTS="$OPTS --skip-narrative"
        [ "${{ inputs.skip-guardrails }}" = "true" ] && OPTS="$OPTS --skip-guardrails"
        [ "${{ inputs.auto-approve }}" = "true" ] && OPTS="$OPTS --auto-approve"
        [ "${{ inputs.post-comment }}" = "true" ] && OPTS="$OPTS --post-comment"
        [ -n "${{ inputs.tag }}" ] && OPTS="$OPTS --tag ${{ inputs.tag }}"

        BUDGET_FLAG=""
        if [ -n "${{ inputs.budget-code }}" ]; then
          BUDGET_FLAG="--budget-code ${{ inputs.budget-code }}"
        fi

        # Run Cost Guardrails — validate cost, post PR comment, cache result
        EXIT_CODE=0
        cost-guardrails \
          --plan "${{ inputs.plan-path }}" \
          $BUDGET_FLAG \
          --format "${{ inputs.format }}" \
          --cache-path cost-guardrails-result.json \
          $OPTS ${{ inputs.extra-args }} || EXIT_CODE=$?

        # Parse decision from exit code
        case $EXIT_CODE in
          0) DECISION="ALLOW" ;;
          2) DECISION="WARN" ;;
          *) DECISION="BLOCK" ;;
        esac

        # Parse monthly cost from cached result
        MONTHLY_COST=""
        if [ -f cost-guardrails-result.json ]; then
          MONTHLY_COST=$(python3 -c "
        import json
        try:
            d = json.load(open('cost-guardrails-result.json'))
            print(d.get('total_monthly_cost', d.get('pricing', {}).get('total_monthly_cost', 'N/A')))
        except: print('N/A')
        " 2>/dev/null || echo "N/A")
        fi

        # Set outputs
        echo "decision=$DECISION" >> $GITHUB_OUTPUT
        echo "monthly_cost=$MONTHLY_COST" >> $GITHUB_OUTPUT
        echo "result_file=cost-guardrails-result.json" >> $GITHUB_OUTPUT
        echo "report_file=cost-guardrails-report.html" >> $GITHUB_OUTPUT

        # Generate HTML report from cache
        if [ -f cost-guardrails-result.json ]; then
          cost-guardrails \
            --cached cost-guardrails-result.json \
            --format html \
            --output-file cost-guardrails-report.html 2>/dev/null || true
        fi

        # Exit with original code (WARN=2 is still success for GitHub)
        if [ $EXIT_CODE -eq 2 ]; then
          echo "::warning::Cost Guardrails decision: WARN — review recommended"
          exit 0
        fi
        exit $EXIT_CODE

    - name: Upload Cost Guardrails Report
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: cost-guardrails-report
        path: |
          cost-guardrails-result.json
          cost-guardrails-report.html
        if-no-files-found: ignore
